# 定时任务功能使用指南

> CarefreeCMS 定时任务管理系统完整文档

**版本**: v1.0.0
**最后更新**: 2025-11-08

---

## 📋 功能概述

定时任务系统允许您自动化执行重复性任务，如数据库备份、缓存清理、日志维护等。系统基于标准Cron表达式，提供完整的任务管理、监控和日志功能。

### 核心特性

- ✅ **Cron表达式支持** - 完整的标准Cron语法
- ✅ **任务管理** - CRUD操作、状态控制、批量管理
- ✅ **手动执行** - 随时手动触发任务
- ✅ **执行日志** - 详细记录每次执行情况
- ✅ **预设任务** - 内置4种常用系统任务
- ✅ **系统保护** - 系统任务防误删
- ✅ **可视化管理** - 完整的前端管理界面

---

## 🚀 快速开始

### 1. 访问管理界面

登录后台 → 系统管理 → 定时任务

### 2. 创建第一个任务

1. 点击「新建任务」按钮
2. 填写任务信息：
   - **任务名称**: `test_backup`（唯一标识）
   - **任务标题**: `测试备份任务`
   - **Cron表达式**: `0 2 * * *`（每天凌晨2点）
   - **执行命令**: 选择「数据库备份」
   - **命令参数**: `{}`（无参数）
3. 点击「确定」创建

### 3. 配置Linux Crontab

```bash
# 编辑crontab
crontab -e

# 添加以下行（每分钟检查一次待执行任务）
* * * * * cd /path/to/cms/backend && php think cron:run >> /dev/null 2>&1
```

---

## 📚 Cron表达式说明

### 格式

```
* * * * *
│ │ │ │ │
│ │ │ │ └─── 星期 (0-7, 0和7都表示周日)
│ │ │ └───── 月份 (1-12)
│ │ └─────── 日期 (1-31)
│ └───────── 小时 (0-23)
└─────────── 分钟 (0-59)
```

### 常用示例

| 表达式 | 说明 | 适用场景 |
|--------|------|---------|
| `* * * * *` | 每分钟 | 测试、高频任务 |
| `*/5 * * * *` | 每5分钟 | 定时发布文章 |
| `*/10 * * * *` | 每10分钟 | 数据同步 |
| `0 * * * *` | 每小时 | 数据统计 |
| `0 0 * * *` | 每天凌晨0点 | 日报生成 |
| `0 2 * * *` | 每天凌晨2点 | 数据库备份 |
| `0 0 * * 1` | 每周一凌晨 | 周报生成 |
| `0 3 * * 0` | 每周日凌晨3点 | 缓存清理 |
| `0 0 1 * *` | 每月1号凌晨 | 月报生成 |

### 特殊字符

- `*` - 任意值
- `,` - 值列表（如 `1,3,5`）
- `-` - 值范围（如 `1-5`）
- `/` - 步长值（如 `*/5` 每5个单位）

---

## 🔧 内置任务类型

### 1. 数据库备份 (`database:backup`)

**功能**: 自动备份MySQL数据库

**参数**:
```json
{
  "tables": []  // 空数组表示备份所有表
}
```

**推荐配置**:
- Cron表达式: `0 2 * * *`（每天凌晨2点）
- 状态: 启用

---

### 2. 缓存清理 (`cache:clear`)

**功能**: 清理系统缓存

**参数**:
```json
{
  "type": "all"  // all-所有, page-页面, data-数据, template-模板
}
```

**推荐配置**:
- Cron表达式: `0 3 * * 0`（每周日凌晨3点）
- 状态: 启用

---

### 3. 日志清理 (`log:clean`)

**功能**: 清理旧的任务执行日志

**参数**:
```json
{
  "days": 30  // 保留天数
}
```

**推荐配置**:
- Cron表达式: `0 4 * * 0`（每周日凌晨4点）
- 状态: 启用

---

### 4. 文章定时发布 (`article:publish`)

**功能**: 发布定时文章（预留功能）

**参数**:
```json
{}
```

**推荐配置**:
- Cron表达式: `*/10 * * * *`（每10分钟）
- 状态: 按需启用

---

## 💻 API使用说明

### 前端API封装

```javascript
import {
  getCronJobList,
  createCronJob,
  updateCronJob,
  deleteCronJob,
  updateCronJobStatus,
  runCronJob,
  getJobLogs
} from '@/api/cronJob'
```

### 使用示例

#### 1. 获取任务列表

```javascript
const response = await getCronJobList({
  page: 1,
  limit: 15,
  is_enabled: 1,  // 筛选启用的任务
  is_system: 0    // 筛选自定义任务
})

console.log(response.data.list)  // 任务列表
console.log(response.data.total) // 总数量
```

#### 2. 创建任务

```javascript
const jobData = {
  name: 'daily_report',
  title: '每日报表生成',
  cron_expression: '0 1 * * *',
  command: 'database:backup',
  params: JSON.stringify({ tables: [] }),
  description: '每天凌晨1点生成日报',
  is_enabled: 1
}

const response = await createCronJob(jobData)
console.log(response.data)  // 新创建的任务
```

#### 3. 更新任务状态

```javascript
// 启用任务
await updateCronJobStatus(jobId, 1)

// 禁用任务
await updateCronJobStatus(jobId, 0)
```

#### 4. 手动执行任务

```javascript
const response = await runCronJob(jobId)
console.log(response.data.output)  // 执行输出
```

#### 5. 查看执行日志

```javascript
const logs = await getJobLogs(jobId, 50)  // 最近50条日志
console.log(logs.data)
```

---

## 🎨 前端页面功能

### 任务列表页面

**路径**: `/system/cron-jobs`

**功能**:
1. **搜索筛选**
   - 按任务名称、标题搜索
   - 按状态、类型筛选

2. **批量操作**
   - 批量删除任务
   - 查看全部日志

3. **单个操作**
   - 启用/禁用任务
   - 立即执行任务
   - 编辑任务信息
   - 查看执行日志
   - 删除任务

### 任务表单

**功能**:
- 创建/编辑任务
- Cron表达式验证（实时验证）
- 预设任务模板
- Cron表达式快捷选择器

---

## 📊 任务状态说明

### 启用状态

- **启用 (is_enabled=1)**: 任务会按照Cron表达式自动执行
- **禁用 (is_enabled=0)**: 任务不会自动执行，但可以手动触发

### 执行状态

- **成功 (success)**: 任务执行成功
- **失败 (failed)**: 任务执行失败，查看错误信息
- **运行中 (running)**: 任务正在执行中

### 任务类型

- **系统任务 (is_system=1)**: 预设的系统任务，不可删除
- **自定义任务 (is_system=0)**: 用户创建的任务，可以删除

---

## 🔒 安全建议

### 1. 任务权限

- 系统任务受保护，不能删除
- 只有管理员可以管理任务
- 建议定期审查任务列表

### 2. 执行安全

- 谨慎配置高频任务（避免系统负载过高）
- 重要任务设置通知机制
- 定期检查任务执行日志

### 3. 数据安全

- 数据库备份任务建议设置为系统任务
- 备份文件定期转移到安全位置
- 定期测试备份恢复流程

---

## 🛠 故障排除

### 问题1: 任务不执行

**症状**: 启用的任务到时间不执行

**排查**:
1. 检查Linux Crontab是否配置正确
2. 检查任务的next_run_time字段
3. 查看系统日志是否有错误
4. 手动执行 `php think cron:run` 测试

**解决**:
```bash
# 检查crontab
crontab -l

# 如果没有配置，添加
* * * * * cd /path/to/cms/backend && php think cron:run >> /dev/null 2>&1
```

---

### 问题2: 任务执行失败

**症状**: 任务状态显示"失败"

**排查**:
1. 查看任务执行日志的error_message字段
2. 检查命令参数是否正确
3. 检查相关服务是否正常（如数据库、缓存）

**解决**:
- 根据错误信息修正配置
- 手动执行测试
- 检查PHP错误日志

---

### 问题3: Cron表达式无效

**症状**: 创建/编辑任务时提示表达式不正确

**排查**:
1. 检查表达式格式是否正确
2. 使用在线工具验证表达式
3. 参考常用表达式示例

**解决**:
- 使用系统提供的快捷选择器
- 参考本文档的表达式示例
- 使用预设任务模板

---

## 📝 最佳实践

### 1. 任务命名规范

```
模块_功能_频率
示例:
- database_backup_daily
- cache_clear_weekly
- log_clean_weekly
```

### 2. 合理安排执行时间

```
凌晨1点 - 数据统计
凌晨2点 - 数据库备份
凌晨3点 - 缓存清理
凌晨4点 - 日志清理
凌晨5点 - 报表生成
```

### 3. 参数配置建议

```json
{
  "days": 30,        // 日志保留天数建议30天
  "tables": [],      // 备份所有表
  "type": "all"      // 清理所有缓存类型
}
```

### 4. 监控与维护

- 定期检查执行日志（每周）
- 清理30天前的日志（自动任务）
- 测试关键任务的执行结果
- 备份任务配置

---

## 🎯 高级用法

### 扩展自定义任务

如需添加新的任务类型，修改以下文件：

**后端**: `backend/app/service/CronJobService.php`

```php
protected function executeJob($job)
{
    switch ($command) {
        // 添加新命令
        case 'your:command':
            return $this->executeYourCommand($params);
        default:
            throw new Exception('不支持的命令类型');
    }
}

// 实现执行方法
protected function executeYourCommand($params)
{
    // 您的业务逻辑
    return [
        'success' => true,
        'output'  => '执行结果'
    ];
}
```

**前端**: `frontend/src/views/system/CronJob.vue`

在命令选择器中添加新选项：

```vue
<el-select v-model="form.command">
  <el-option label="您的任务" value="your:command" />
</el-select>
```

---

## 📞 技术支持

- **文档**: [docs/CRON_JOB_GUIDE.md](./CRON_JOB_GUIDE.md)
- **问题反馈**: https://gitee.com/carefreeteam/issues
- **邮箱**: sinma@qq.com
- **QQ群**: 113572201

---

## 📄 附录

### 数据库表结构

#### cron_jobs（任务表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int | 主键 |
| name | varchar(100) | 任务名称（唯一） |
| title | varchar(255) | 任务标题 |
| cron_expression | varchar(100) | Cron表达式 |
| command | varchar(500) | 执行命令 |
| params | text | 命令参数（JSON） |
| is_enabled | tinyint | 启用状态 |
| is_system | tinyint | 是否系统任务 |
| run_count | int | 运行次数 |
| last_run_time | datetime | 最后运行时间 |
| last_run_status | varchar(20) | 最后运行状态 |
| last_run_duration | int | 最后运行时长（秒） |
| next_run_time | datetime | 下次运行时间 |
| description | varchar(500) | 任务描述 |

#### cron_job_logs（日志表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int | 主键 |
| job_id | int | 任务ID |
| job_name | varchar(100) | 任务名称 |
| status | varchar(20) | 执行状态 |
| start_time | datetime | 开始时间 |
| end_time | datetime | 结束时间 |
| duration | int | 运行时长（秒） |
| output | text | 输出信息 |
| error_message | text | 错误信息 |

---

**Made with ❤️ by CarefreeCMS Team © 2025**
